import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-rick-and-morty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
